<?php
// Edita una materia existente
if( isset( $_POST['agregar-tema-enviado'] ) ) {
	
	echo 'EDITANDO MATERIA EXISTENTE';
	
	$output = '';
	
	if( isset( $_POST['modulo'] ) ) {
		
	} // if( isset( $_POST['modulo'] ) ) {
	
} // if( isset( $_POST['agregar-tema-enviado'] ) ) {
?>